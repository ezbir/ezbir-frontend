export const api = "http://localhost:8080"

export const apiPrefix = "/api/"

export const authPrefix = apiPrefix + "/auth"

export const fundraiserPrefix = apiPrefix + "/fundraiser"

export const userPrefix = apiPrefix + "/user"

export const postPrefix = apiPrefix + "/post"

export const apiPath = {
    //auth
    login: authPrefix + "/login",
    register: authPrefix + "/register",
    verify: authPrefix + "/verify",

    //fundraiser
}
